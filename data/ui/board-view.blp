using Gtk 4.0;
using Adw 1;

template $StormBoardView: Frame {
    Box {
        orientation: vertical;

        Adw.ActionRow name-row {
            title: "Name";

            styles [
                "property"
            ]
        }

        Grid grid {}
    }
}
